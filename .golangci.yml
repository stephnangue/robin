version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
  exclusions:
    rules:
      # Unchecked Close() on response/request bodies in tests is noise.
      - path: _test\.go
        linters:
          - errcheck
